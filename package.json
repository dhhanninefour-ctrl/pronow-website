{
  "name": "pronow-website",
  "version": "1.0.0",
  "private": true,
  "description": "PRONOW company website (static HTML/CSS/JS)",
  "scripts": {
    "vercel-build": "echo \"Static site — no build step needed.\"",
    "build": "echo \"Static site — no build step needed.\""
  }
}
